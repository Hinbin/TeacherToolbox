<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TeacherToolbox.Controls.RandomNameGenerator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TeacherToolbox.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    KeyDown="Page_KeyDown"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Viewbox Grid.Row="0" Tapped="NameDisplay_Tapped">
            <TextBlock x:Name="NameDisplay" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Viewbox>
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto">
            <StackPanel x:Name="ClassList"  Orientation="Horizontal" Padding="5" Margin="5" Spacing="5" >
                <Button x:Name="AddClassButton" Content="Add Class" Click="Add_Class_Clicked" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
