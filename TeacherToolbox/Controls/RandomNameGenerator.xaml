<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TeacherToolbox.Controls.RandomNameGenerator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TeacherToolbox.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Enabled"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Viewbox  Grid.Row="0" Tapped="NameDisplay_Tapped">
            <TextBlock x:Name="NameDisplay" Margin="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Viewbox>
        <Viewbox Grid.Row="1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalAlignment="Bottom">
                <StackPanel x:Name="ClassList"  Orientation="Horizontal" VerticalAlignment="Bottom" Spacing="5" Margin="5">
                    <Button x:Name="AddClassButton" Content="Add Class" Click="Add_Class_Clicked" />
                </StackPanel>
            </ScrollViewer>
        </Viewbox>
    </Grid>
</Page>
