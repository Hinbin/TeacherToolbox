<Page
    x:Class="TeacherToolbox.Controls.ScreenRulerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TeacherToolbox.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Viewbox>
            <Grid Width="200">
                <!-- Set the Grid width inside the Viewbox -->
                <!-- Define rows for each button -->
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Set a standard width for each button and remove individual paddings and margins if desired -->
                <Button x:Name="CloseRulerWindowButton" Click="Close_Ruler_Window_Click" Content="Close Screen Ruler" Grid.Row="0" Width="150" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                <Button x:Name="OpenRulerWindowButton" Click="Open_Ruler_Window_Click" Content="Open Screen Ruler" Grid.Row="1" Width="150" Margin="0,0,0,10" HorizontalAlignment="Center" Visibility="Collapsed"/>
                <Button x:Name="ChangeDisplayButton" Click="ChangeDisplayButton_Click" Content="Change Display" Grid.Row="2" Width="150" HorizontalAlignment="Center" Visibility="Collapsed"/>
            </Grid>
        </Viewbox>
    </Grid>
</Page>
