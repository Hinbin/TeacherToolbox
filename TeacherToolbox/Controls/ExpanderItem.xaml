<UserControl
    x:Class="TeacherToolbox.Controls.ExpanderItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16,12" ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock
                x:Name="HeaderText"
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="{x:Bind Header, Mode=OneWay}" />
            <TextBlock
                x:Name="SubheaderText"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind Subheader, Mode=OneWay}"
                Visibility="{x:Bind HasSubheader, Mode=OneWay}" />
        </StackPanel>

        <ContentPresenter
            Grid.Column="1"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Content="{x:Bind ItemContent, Mode=OneWay}" />
    </Grid>
</UserControl>